Desktop: Each account on the Accounts page can be kept warm: with it on, swapd starts the account's 5h window whenever it has gone cold, so a switch onto it lands on a clock already running (needs swapd 0.3).
Phone: The Accounts page offers the same keep-warm toggle per account.
