Mac: A sign-in routed to Chrome or Edge opens in a browser instance of its own on an empty profile, so the account already signed in there never gets in the way.
Desktop: The same for the desktop app's own sign-in.
