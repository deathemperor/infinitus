Mac: a freshly installed engine now reports the provider it manages before its first account, so Accounts offers the first sign-in instead of telling you to install the engine you just installed.
Desktop: Accounts offers Add account on a fleet that signs in through its CLI, not only on engines with an OAuth flow of their own.
