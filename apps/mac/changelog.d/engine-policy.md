Desktop: Settings → Engines shows swapd's switching policy (on/off, strategy, threshold, the models whose weekly limits count) for each machine and lets you change it there.
Mac: the control socket gains policy, policy-set and policy-unset, the engine's own config knobs read and written in its words.
