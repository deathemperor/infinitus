Mac: The all-limited banner and push say which model ran out ("out of Fable") when only that model's window is spent.
Desktop: The accounts page's exhausted band names the model when one per-model window alone blocks every account.
Phone: The accounts screen's exhausted band names the model when one per-model window alone blocks every account.
