Desktop: Settings → Engines checks swapd against its newest release and installs it on that Mac with one click, without waiting for an Infinitus release.
Mac: the control socket gains engine-update-check and engine-update; a release installed that way runs ahead of the bundled engine while it is the newer one.
