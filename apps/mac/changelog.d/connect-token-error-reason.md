Desktop: When Infinitus Connect cannot get its session token, the error now says why instead of only that it failed.
Phone: When Infinitus Connect cannot get its session token, the error now says why instead of only that it failed.
