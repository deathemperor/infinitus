Mac: infinitusctl thread new starts a thread on a project without its own default model by taking the environment's, and --model <instance>/<model> (or a bare <model>) picks one outright; the refusal now says where to set a default.
