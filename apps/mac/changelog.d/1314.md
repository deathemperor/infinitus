Phone: A thread card the Mac starts while the app is in the background asks the phone to reconnect at once, so its token reaches the Mac inside that window instead of when the app is next opened; the Card push registration row says how long it took.
