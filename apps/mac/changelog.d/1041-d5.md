Mac: The session tracker is gone: no live session list beyond the snapshot's count, no feed, chat, timeline, attention, names or input from the Mac, the phone or `infinitusctl`.
Mac: The browser page ("Copy Browser Link") is gone.
Mac: Keep awake is gone (Settings › Display's two switches and the `keep_awake` prefs); the desktop app holds sleep off while a turn runs.
Mac: Expired `aws login` / `gcloud auth login` are no longer detected from terminal sessions; `aws-login` / `gcloud-login` lose `--pid`, and sign-ins started from the desktop app or the phone work as before.
Mac: The old native phone client's sources (`apps/mac/ios`) are removed.
