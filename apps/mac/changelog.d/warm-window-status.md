Mac: A keep-warm account whose 5h window has stopped now says `cold` where its countdown would be, instead of a blank that looked identical to a running window, and one confirmed click starts it.
Mac: Keep-warm is on the account row's context menu, so the popup can turn it on and off.
