Desktop: The local environment can be turned off in Settings › Connections, so a computer only drives work running elsewhere; switch it back on in the same place.
Linux: `t3 update` points the background service at the new version even when you decline the restart, and `t3 service restart` picks it up later.
Desktop: A device host that resolves to this computer is skipped, and testing a host runs against the selected environments.
