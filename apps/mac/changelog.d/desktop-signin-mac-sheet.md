Desktop: With Safari as the default browser, the desktop app's sign-in hands off to the menu bar app's private sign-in sheet, so passkeys work and no signed-in account gets in the way.
Mac: `signin-begin --window` runs a sign-in with the Mac's own sheet for a caller on the same machine.
