Mac: The menu bar app runs the account engine shipped inside it, not an older copy left on your PATH, so a fixed sign-in is fixed after the update.
Desktop: The same for the desktop app's own sign-in.
