Desktop: Claude threads show how long the prompt cache stays warm — a green composer border and a countdown by the send button, amber near the end — so you can tell whether the next message reuses the cache or sends the whole context again.
Phone: Thread usage shows the same prompt cache countdown.
