Server: the command-line server is `infinitus` now (`infinitus serve`, `infinitus update`, `infinitus service install`); the installer links that name, and a release still carries the archive under its old name for one version so an earlier install can update.
