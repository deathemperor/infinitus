# TASKS — Infinitus on Windows

Dependency-ordered. Sizes: S ≤ ½ day, M ≤ 2 days, L > 2 days. Paths are
repo-relative; daemon sources live under `windows/` (new, root-level).
Every task: commit locally, never push (CLAUDE.md). Stage by explicit
path.

| # | task | scope | files | acceptance | size | deps |
|---|---|---|---|---|---|---|
| W1 | Core compiles + tests on Windows | Fence `PeerSocket.swift` imports (`#elseif canImport(Glibc)`) and `write` body (`#if os(Windows) return false`); Windows `ClaudeSessions.isAlive` via `OpenProcess`/`GetExitCodeProcess`; `SessionInput.defaultAttachmentsDir` `%LOCALAPPDATA%\Infinitus\attachments`; fence `SessionResumeTests.swift` (Glibc + AF_UNIX tests) and `CLIProxyEngineTests.swift` (`FoundationNetworking` or `#if canImport(Darwin)`) | `Sources/InfinitusCore/PeerSocket.swift`, `Sources/InfinitusCore/ClaudeSessions.swift`, `Sources/InfinitusCore/SessionInput.swift`, `Tests/InfinitusCoreTests/SessionResumeTests.swift`, `Tests/InfinitusCoreTests/CLIProxyEngineTests.swift` | On this box: `swift build --product InfinitusCore` and `swift test` exit 0; on macOS `swift test` unchanged | S | — |
| W2 | Package target + env scripts | `#if os(Windows)` `InfinitusWin` executable target (`ws2_32`, `dnsapi`, `iphlpapi`) + `InfinitusWinTests`; `windows/env.ps1` (PATH/SDKROOT), `windows/build.ps1`; fence anything in `InfinitusTray` that fails on Windows | `Package.swift`, `windows/env.ps1`, `windows/build.ps1`, `windows/Sources/InfinitusWin/main.swift` (hello), `windows/Tests/InfinitusWinTests/` | `swift build --product infinitus-win` exit 0; `.\.build\debug\infinitus-win.exe --version` prints | S | W1 |
| W3 | Windows process + session listing | `WinProcess.swift` (`isAlive(pid:procStart:)` with FILETIME match, `machineName`); `sessions` subcommand printing pid/status/alive/pipe/cwd; `WaitNamedPipeW(0)` pipe liveness | `windows/Sources/InfinitusWin/WinProcess.swift`, `windows/Sources/InfinitusWin/NamedPipeClient.swift` (liveness half), `main.swift` | `infinitus-win sessions` lists every live `claude.exe` (7 today), alive+pipe true; a stale record with a reused pid is excluded; `WinProcessTests` pass | S | W2 |
| W4 | Winsock HTTP/1.1 listener | Port of `PosixHTTPServer` (thread per connection, authorize-before-body, body cap by route, 5 s recv timeout, `Connection: close`) | `windows/Sources/InfinitusWin/WinHTTPServer.swift`, `windows/Tests/InfinitusWinTests/WinHTTPServerTests.swift` | `WinHTTPServerTests` = `PosixHTTPServerTests` set green on loopback | M | W2 |
| W5 | Pairing token store + `pair` | `%APPDATA%\Infinitus\pair-token` user-only DACL, `--token-file`/stdin, masked print; `pair` prints `MirrorPairing.pairURL(endpoints:token:)` from `GetAdaptersAddresses` LAN + tailnet (`MirrorPairing.lanAddress/tailnetAddress`); QR via `qrencode` if on PATH | `windows/Sources/InfinitusWin/WinPairingStore.swift`, `windows/Sources/InfinitusWin/WinAddresses.swift`, `main.swift` | `pair` prints an `infinitus://pair?url=http://<lan>:47824&token=…` URL; token file reused across runs; `PairingStoreTests` pass | S | W2 |
| W6 | Snapshot builder | `MirrorSnapshot` with synthetic fleet `claude-code-windows`/`.claude`, `accounts: []`, `liveSessions` (+breakdown), `sessions` rows via `SessionPanelRow.make`, `progressByPid` via `SessionProgress.read`, `listJSON` = encoded `AccountList`; 5 s cache; `.iso8601` | `windows/Sources/InfinitusWin/Snapshot.swift`, `windows/Tests/InfinitusWinTests/SnapshotTests.swift` | `SnapshotTests.testDecodesOnPhoneShape`; `infinitus-win snapshot` prints JSON with `fleets[0].liveSessions.total` ≥ 1 | S | W3 |
| W7 | `serve`: snapshot + tail + images + stubs | Route handler mirroring `Sources/InfinitusTray/InfinitusTray.swift:641-696` plus images (`SessionFeedReader.imageData`, original bytes, 5 MiB cap), `POST /activities/token` → 204, aws-login → 404, input → `noChannel` stub; firewall hint on start; `--port`, `--claude-dir` | `windows/Sources/InfinitusWin/Routes.swift`, `main.swift`, `windows/run.ps1` | `windows/smoke.ps1` steps 1–6 PASS (401/200 snapshot, feed, long-poll wakes < 1 s on a typed line, image bytes) | M | W4, W5, W6 |
| W8 | Windows path rules in core image ids | `attachedImageIds` splits on `/` and `\`; `imageData` refuses `\` and `:` in `a:` names | `Sources/InfinitusCore/SessionFeed.swift`, `Tests/InfinitusCoreTests/SessionFeedTests.swift` | New `SessionFeedTests` cases pass on macOS and Windows; Mac behaviour unchanged (existing tests green) | S | W1 |
| W9 | Additive feed fields | `SessionFeed.canMessage: Bool?`, `keys: Bool?`, `permissionMode: String?` (newest `permission-mode` entry's `permissionMode` in the tail); Mac sets `keys: true`, `canMessage` from socket path, Windows from `WaitNamedPipeW` | `Sources/InfinitusCore/SessionFeed.swift`, `Sources/Infinitus/AppModel.swift` (feed provider), `windows/Sources/InfinitusWin/Routes.swift`, tests | Old phone JSON decodes (fields optional); `SessionFeedTests` covers `permission-mode` parsing | S | W7, W8 |
| W10 | Named-pipe input injection | `NamedPipeClient.write` (`WaitNamedPipeW` → `CreateFileW` → `WriteFile` → `FlushFileBuffers`); `send` = `PeerSocket.frames` + write with `ownAddress` `uds:\\.\pipe\LOCAL\infinitus-<pid>`; wire `SessionInput.deliver(hosts: [], socketSend:)` into `POST /sessions/<pid>/input`; attachments to `%LOCALAPPDATA%\Infinitus\attachments`; log line per delivery | `windows/Sources/InfinitusWin/NamedPipeClient.swift`, `Routes.swift`, `windows/Tests/InfinitusWinTests/NamedPipeTests.swift` | `NamedPipeTests` round-trip byte-equal; smoke steps 7–9: message shows in the terminal and the feed renders `Infinitus app: ping from smoke`; resume delivered; key → `noSurface` | M | W7 |
| W11 | Bonjour advertise | `DnsServiceRegister` for `<machineName>._infinitus._tcp.local:47824`; graceful fallback message when registration fails; deregister on exit | `windows/Sources/InfinitusWin/WinBonjour.swift`, `main.swift` | Phone on the same Wi-Fi discovers the host without a typed address; `dns-sd -B _infinitus._tcp` shows it | M | W7 |
| W12 | Custom-API acceptance run | Execute `05-custom-api.md` steps 1–3 with the proxy config on this box; record results in `windows/README.md` | `windows/README.md` | All three steps recorded PASS with dates | S | W10 |
| W13 | Phone: multi-host storage + transport | `MirrorHost` model, `mirror_hosts` defaults key, legacy migration, `NetworkFleetMirror` per-host `latestAll/sessionTail/sessionImage/sessionInput/awsLogin*`, Bonjour-to-host token matching (no 401 stop in discovery), `INFINITUS_MIRROR_PATHS` dev seam | `ios/InfinitusMobile/NetworkFleetMirror.swift`, `ios/InfinitusMobile/MirrorModel.swift`, new `ios/InfinitusMobile/MirrorHost.swift` | Simulator with two fixture snapshots shows two hosts; existing single-host defaults migrate into host #0 without re-pairing | M | — (wire-independent; test against W7 for real) |
| W14 | Phone: merged sessions, host-aware feed, settings | `SessionKey`/`HostSession`, per-host sections with emoji+label, `SessionFeedScreen(hostSession:)`, composer gating on `canMessage`/`keys`, held-for-approval wording on `permissionMode == "default"`, thumbnail cache key with host, Settings "Hosts" section (list, detail, emoji palette, scan/add create-or-update, delete), deep-link path, Fleet tab hides `claude-code-*` fleets with no accounts | `ios/InfinitusMobile/SessionsScreen.swift`, `SessionFeedScreen.swift`, `FeedThumbnail.swift`, `SettingsScreen.swift`, `InfinitusMobileApp.swift`, `MirrorFleetModel.swift`, `NativeFleetScreen.swift`, `FleetScreen.swift`, `RootView.swift` | Phone acceptance in `07-testing.md`: Mac + Windows paired side by side, message to a Windows session round-trips, killing the daemon degrades only its section | L | W13, W9 |
| W15 | `resume` CLI (manual nudge) | `infinitus-win resume [--pid N] [--dry-run]` via `Transcript.findStopped` + `ResumeCoordinator(hosts: [])` with the pipe `socketSend` | `windows/Sources/InfinitusWin/main.swift`, `windows/Sources/InfinitusWin/Resume.swift` | `--dry-run` lists a session whose transcript tail is a limit stop (fixture); live run delivers `nudgeText(attempt: 0)` once, retries follow `retryDelays` on `burned` | S | W10 |
| W16 | WIC thumbnails (optional) | Downscale to ≤ 640 px JPEG via `IWICImagingFactory`; fall back to original bytes | `windows/Sources/InfinitusWin/WinThumbnail.swift`, `Routes.swift` | Image route returns `image/jpeg` ≤ 640 px for a 4 K PNG; smoke step 6 still PASS | M | W7 |
| W17 | Release: docs, site, README, version | `windows/README.md` (install `winget install --id Swift.Toolchain -e`, build, firewall, pair, serve, `cloudflared` optional), root `README.md` Windows section, `site/` feature entry, `ios/project.yml` `MARKETING_VERSION` 0.5.0; update `docs/TODO.md` | `windows/README.md`, `README.md`, `site/src/…`, `ios/project.yml`, `docs/TODO.md` | All four updated in the same release commit (CLAUDE.md release rule) | S | W11, W14 |
| W18 | `windows/smoke.ps1` + `windows/ci.ps1` | Scripted `07-testing.md` integration steps 1–11 against `--port 47825`; CI script = build + both test suites | `windows/smoke.ps1`, `windows/ci.ps1` | `smoke.ps1` prints 11 PASS lines on this box | S | W10, W11 |

Order of execution: W1 → W2 → (W3, W4, W5 in parallel) → W6 → W7 → W8 →
W9 → W10 → W11 → W12 → W18; phone track W13 → W14 can start after W7 (needs
a live daemon to test against) and merges with W9; W15/W16 anytime after
W10/W7; W17 last.

Critical-path estimate: W1–W7 ≈ 5 days (read-only remote usable), W8–W12
≈ 3 days (input), W13–W14 ≈ 5 days (phone), W17–W18 ≈ 1 day.
