# The fork's own desktop release: a macOS arm64 DMG of `main`, published as a
# GitHub prerelease tagged `v<version>-infinitus.<date>.<run>` on the
# `infinitus` updater channel. Upstream's release.yml stays untouched (it also
# publishes npm, AUR, web and marketing, and is disabled in this repo), and the
# native Infinitus app's channels — `releases/latest` and the `nightly` tag —
# are never written here.
name: Fork desktop release
on:
  workflow_dispatch:
    inputs:
      publish:
        description: "Publish a prerelease (off = build only, DMG as a workflow artifact)"
        type: boolean
        default: true
      native_helper_tag:
        description: "Native release tag to nest as the menu bar helper (blank = the pin in apps/desktop/native-helper.json, none = nest nothing)"
        type: string
        default: ""

jobs:
  build_mac_arm64:
    name: Build macOS arm64
    runs-on: macos-latest
    timeout-minutes: 45
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          sparse-checkout: |
            /*
            !/.repos/
          sparse-checkout-cone-mode: false

      - name: Setup Vite+
        uses: voidzero-dev/setup-vp@v1
        with:
          node-version-file: package.json
          cache: true
          run-install: false

      - name: Install desktop dependencies
        run: vp install --filter=@t3tools/desktop... --filter=t3... --filter=@t3tools/scripts...

      - name: Cache resource monitor
        id: resource_monitor_cache
        uses: actions/cache@v6
        with:
          path: native/resource-monitor/target/aarch64-apple-darwin/release/t3-resource-monitor
          key: resource-monitor-aarch64-apple-darwin-${{ hashFiles('native/resource-monitor/Cargo.lock', 'native/resource-monitor/Cargo.toml', 'native/resource-monitor/src/**') }}

      - name: Setup Rust
        if: steps.resource_monitor_cache.outputs.cache-hit != 'true'
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-apple-darwin

      # The native Infinitus menu bar app rides inside the desktop bundle as a
      # login item (#777). It is never rebuilt here: the native branch's
      # release.yml signs, notarizes and staples the .app, so the fetched zip
      # already carries a helper that passes Gatekeeper on its own. The pin
      # (`{"tag": "v1.2.3", "sha256": "…"}`) is bumped by PR; a dispatch may
      # try another tag before pinning it.
      - name: Resolve native helper
        id: native_helper_pin
        shell: bash
        env:
          TAG_OVERRIDE: ${{ inputs.native_helper_tag }}
        run: |
          set -euo pipefail
          tag="$TAG_OVERRIDE"
          sha256=""
          if [[ -z "$tag" && -f apps/desktop/native-helper.json ]]; then
            tag=$(jq -r '.tag // ""' apps/desktop/native-helper.json)
            sha256=$(jq -r '.sha256 // ""' apps/desktop/native-helper.json)
          fi
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "sha256=$sha256" >> "$GITHUB_OUTPUT"
          if [[ -z "$tag" ]]; then
            echo "No native helper pinned; the desktop nests no menu bar app." >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Fetch native helper
        id: native_helper
        if: ${{ steps.native_helper_pin.outputs.tag != '' }}
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.native_helper_pin.outputs.tag }}
          SHA256: ${{ steps.native_helper_pin.outputs.sha256 }}
        run: |
          set -euo pipefail
          dir="$RUNNER_TEMP/native-helper"
          mkdir -p "$dir"
          gh release download "$TAG" -R "$GITHUB_REPOSITORY" -p 'Infinitus-*.zip' -D "$dir"
          zip=$(ls "$dir"/Infinitus-*.zip)
          if [[ -n "$SHA256" ]]; then
            echo "$SHA256  $zip" | shasum -a 256 -c -
          fi
          ditto -x -k "$zip" "$dir/unpacked"
          app=$(find "$dir/unpacked" -maxdepth 2 -name '*.app' -type d | head -n 1)
          if [[ -z "$app" || ! -f "$app/Contents/Info.plist" ]]; then
            echo "The native release $TAG carries no app bundle." >&2
            exit 1
          fi
          bundle_id=$(/usr/libexec/PlistBuddy -c 'Print CFBundleIdentifier' "$app/Contents/Info.plist")
          version=$(/usr/libexec/PlistBuddy -c 'Print CFBundleShortVersionString' "$app/Contents/Info.plist")
          if [[ "$bundle_id" != "run.infinitus" ]]; then
            echo "The native app's bundle id is $bundle_id, not run.infinitus." >&2
            exit 1
          fi
          echo "path=$app" >> "$GITHUB_OUTPUT"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "Nesting the native Infinitus $version ($TAG) as the menu bar helper." >> "$GITHUB_STEP_SUMMARY"

      - name: Resolve fork release version
        id: version
        shell: bash
        run: |
          set -euo pipefail
          base=$(node -p "require('./apps/desktop/package.json').version")
          version="${base}-infinitus.$(date -u +%Y%m%d).${GITHUB_RUN_NUMBER}"
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "tag=v$version" >> "$GITHUB_OUTPUT"
          echo "Building Infinitus Desktop $version." >> "$GITHUB_STEP_SUMMARY"

      - name: Align package versions to release version
        run: node scripts/update-release-package-versions.ts "${{ steps.version.outputs.version }}"

      # The certificate and its password reach electron-builder through the
      # build step's own env; only the notary key needs a file on disk, and
      # `security cms` never sees it — notarytool reads the .p8 directly.
      - name: Prepare macOS signing
        id: signing
        shell: bash
        env:
          DEVELOPER_ID_P12_BASE64: ${{ secrets.DEVELOPER_ID_P12_BASE64 }}
          DEVELOPER_ID_P12_PASSWORD: ${{ secrets.DEVELOPER_ID_P12_PASSWORD }}
          NOTARY_KEY_BASE64: ${{ secrets.NOTARY_KEY_BASE64 }}
          NOTARY_KEY_ID: ${{ secrets.NOTARY_KEY_ID }}
          NOTARY_ISSUER_ID: ${{ secrets.NOTARY_ISSUER_ID }}
        run: |
          set -euo pipefail
          if [[ -z "$DEVELOPER_ID_P12_BASE64" || -z "$DEVELOPER_ID_P12_PASSWORD" ]]; then
            echo "signed=false" >> "$GITHUB_OUTPUT"
            echo "macOS signing disabled (no Developer ID certificate secret); building unsigned." \
              >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          if [[ -z "$NOTARY_KEY_BASE64" || -z "$NOTARY_KEY_ID" || -z "$NOTARY_ISSUER_ID" ]]; then
            echo "A Developer ID certificate is configured but the notary key secrets are not." >&2
            exit 1
          fi

          key_path="$RUNNER_TEMP/AuthKey_${NOTARY_KEY_ID}.p8"
          printf '%s' "$NOTARY_KEY_BASE64" | base64 -D > "$key_path"
          chmod 600 "$key_path"
          echo "notary_key_path=$key_path" >> "$GITHUB_OUTPUT"

          # Import the certificate into a keychain of our own and hand
          # electron-builder that keychain (CSC_KEYCHAIN) instead of the p12
          # (CSC_LINK): electron-builder 26.15's own import creates the
          # keychain with a random password but then runs
          # `set-key-partition-list -k <p12 password>` against it, which fails
          # with "passphrase you entered is not correct" (run 34488667586).
          # Same sequence as the native branch's release workflow.
          keychain="$RUNNER_TEMP/fork-sign.keychain-db"
          p12_path="$RUNNER_TEMP/developer-id.p12"
          printf '%s' "$DEVELOPER_ID_P12_BASE64" | base64 -D > "$p12_path"
          security create-keychain -p "" "$keychain"
          security set-keychain-settings -lut 21600 "$keychain"
          security unlock-keychain -p "" "$keychain"
          security import "$p12_path" -k "$keychain" -P "$DEVELOPER_ID_P12_PASSWORD" \
            -T /usr/bin/codesign -T /usr/bin/productbuild
          security set-key-partition-list -S apple-tool:,apple: -s -k "" "$keychain"
          security list-keychains -d user -s "$keychain" \
            $(security list-keychains -d user | tr -d '"')
          rm -f "$p12_path"
          if ! security find-identity -v -p codesigning "$keychain" | grep -q "Developer ID Application"; then
            echo "The imported certificate carries no Developer ID Application identity." >&2
            exit 1
          fi
          echo "keychain=$keychain" >> "$GITHUB_OUTPUT"
          echo "signed=true" >> "$GITHUB_OUTPUT"
          echo "macOS signing enabled (Developer ID + notarytool)." >> "$GITHUB_STEP_SUMMARY"

      # A signed desktop can only nest a helper notarytool accepts: Developer
      # ID from the same team, hardened runtime, and its own stapled ticket
      # (the nested code keeps that ticket; the outer bundle gets its own).
      # Fail here, naming the tag, rather than in notarization.
      - name: Verify native helper signature
        if: ${{ steps.native_helper.outputs.path != '' && steps.signing.outputs.signed == 'true' }}
        shell: bash
        env:
          APP: ${{ steps.native_helper.outputs.path }}
          TAG: ${{ steps.native_helper_pin.outputs.tag }}
        run: |
          set -euo pipefail
          details=$(codesign -dv --verbose=2 "$APP" 2>&1)
          for want in "Authority=Developer ID Application" "TeamIdentifier=Q783W6B4FA" "flags=0x10000(runtime)"; do
            if ! grep -qF "$want" <<<"$details"; then
              echo "The native release $TAG is not signed for nesting (missing: $want); cut a Developer ID release first." >&2
              echo "$details" >&2
              exit 1
            fi
          done
          codesign --verify --strict --verbose=2 "$APP"
          xcrun stapler validate "$APP"

      - name: Build desktop artifact
        shell: bash
        env:
          # The updater feed is this repo, so the published prerelease is the
          # one an installed fork build finds on the `infinitus` channel.
          T3CODE_DESKTOP_UPDATE_REPOSITORY: ${{ github.repository }}
          T3CODE_DESKTOP_REUSE_RESOURCE_MONITOR: ${{ steps.resource_monitor_cache.outputs.cache-hit == 'true' }}
          T3CODE_APPLE_TEAM_ID: Q783W6B4FA
          CSC_KEYCHAIN: ${{ steps.signing.outputs.keychain }}
          APPLE_API_KEY: ${{ steps.signing.outputs.notary_key_path }}
          APPLE_API_KEY_ID: ${{ secrets.NOTARY_KEY_ID }}
          APPLE_API_ISSUER: ${{ secrets.NOTARY_ISSUER_ID }}
          APPLE_TEAM_ID: Q783W6B4FA
          T3CODE_DESKTOP_NATIVE_HELPER: ${{ steps.native_helper.outputs.path }}
        run: |
          set -euo pipefail
          args=(
            --platform mac
            --target dmg
            --arch arm64
            --build-version "${{ steps.version.outputs.version }}"
            --verbose
          )
          if [[ "${{ steps.signing.outputs.signed }}" == "true" ]]; then
            args+=(--signed)
          else
            export CSC_IDENTITY_AUTO_DISCOVERY=false
          fi

          vp run dist:desktop:artifact "${args[@]}"

      # The helper must come out of packaging with its own seal intact and
      # its own entitlements, not Electron's: electron-builder's signIgnore
      # keeps the fork's signer off it, and this proves that held.
      - name: Verify nested helper
        if: ${{ steps.native_helper.outputs.path != '' }}
        shell: bash
        env:
          SIGNED: ${{ steps.signing.outputs.signed }}
        run: |
          set -euo pipefail
          # The zip is what electron-updater installs, so verify that, not
          # the stage (the build script copies only files to release/).
          ditto -x -k release/Infinitus-*.zip "$RUNNER_TEMP/nested-check"
          app=$(find "$RUNNER_TEMP/nested-check" -maxdepth 1 -name '*.app' -type d | head -n 1)
          nested="$app/Contents/Library/LoginItems/Infinitus Menu Bar.app"
          if [[ ! -f "$nested/Contents/Info.plist" ]]; then
            echo "The packaged app carries no nested helper at $nested." >&2
            find "$app/Contents" -maxdepth 2 >&2
            exit 1
          fi
          codesign --verify --strict --verbose=2 "$nested"
          if codesign -d --entitlements :- "$nested" 2>/dev/null | grep -q "com.apple.security.cs.allow-jit"; then
            echo "The nested helper was re-signed with Electron's entitlements." >&2
            exit 1
          fi
          if [[ "$SIGNED" == "true" ]]; then
            codesign --verify --deep --strict --verbose=2 "$app"
          fi

      - name: Remove the signing keychain
        if: ${{ always() && steps.signing.outputs.keychain != '' }}
        shell: bash
        run: security delete-keychain "${{ steps.signing.outputs.keychain }}" || true

      # The DMG installs the app; the zip and `infinitus-mac.yml` are what
      # electron-updater downloads for an in-place update on the `infinitus`
      # channel. One arch, so no per-arch manifest renaming or merging.
      - name: Collect release assets
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p release-publish

          shopt -s nullglob
          for pattern in \
            "release/*.dmg" \
            "release/*.zip" \
            "release/*.blockmap" \
            "release/infinitus-mac.yml"; do
            for file in $pattern; do
              cp "$file" release-publish/
            done
          done

          if [[ ! -f release-publish/infinitus-mac.yml ]]; then
            echo "electron-builder wrote no infinitus-mac.yml; the update feed would be broken." >&2
            ls -1 release >&2
            exit 1
          fi

      - name: Upload build artifacts
        uses: actions/upload-artifact@v7
        with:
          name: infinitus-desktop-mac-arm64
          path: release-publish/*
          if-no-files-found: error

      - name: Publish prerelease
        if: ${{ inputs.publish }}
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.version.outputs.tag }}
          VERSION: ${{ steps.version.outputs.version }}
          HELPER_VERSION: ${{ steps.native_helper.outputs.version }}
          HELPER_TAG: ${{ steps.native_helper_pin.outputs.tag }}
        run: |
          set -euo pipefail
          HELPER_NOTE=""
          if [[ -n "$HELPER_VERSION" ]]; then
            HELPER_NOTE=" Nests the native Infinitus menu bar app ${HELPER_VERSION} (${HELPER_TAG}) as a login item."
          fi
          gh release create "$TAG" \
            --prerelease \
            --target "$GITHUB_SHA" \
            --title "Infinitus Desktop ${VERSION} (${GITHUB_SHA::7})" \
            --notes "Fork desktop build of main at ${GITHUB_SHA::7}; installs beside T3 Code and auto-updates on the infinitus channel.${HELPER_NOTE}" \
            release-publish/*
          echo "Published prerelease $TAG." >> "$GITHUB_STEP_SUMMARY"
